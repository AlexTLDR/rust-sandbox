# Traits Lab

## Running the Examples

### Display Trait Example

```bash
cargo run -p traits --bin display
```

### Main Example (traits binary)

```bash
cargo run -p traits --bin traits
# or simply (now that default-run is set):
cargo run -p traits
```

